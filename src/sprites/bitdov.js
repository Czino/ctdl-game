const w = 32
const h = 48

export default {
  right: {
    move: [
      { x: 0 * w, y: 0 * h, w, h },
      { x: 1 * w, y: 0 * h, w, h },
      { x: 2 * w, y: 0 * h, w, h },
      { x: 3 * w, y: 0 * h, w, h },
      { x: 4 * w, y: 0 * h, w, h },
      { x: 5 * w, y: 0 * h, w, h },
      { x: 6 * w, y: 0 * h, w, h },
      { x: 7 * w, y: 0 * h, w, h },
    ]
  }
}