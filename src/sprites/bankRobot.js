const w = 16
const h = 8

export default {
  right: {
    idle: [
      { x: 0 * w, y: 0 * h, w, h}
    ],
    move: [
      { x: 0 * w, y: 0 * h, w, h}
    ],
    hurt: [
      { x: 0 * w, y: 0 * h, w, h}
    ],
    rekt: [
      { x: 0 * w, y: 0 * h, w, h}
    ],
    alarm: [
      { x: 0 * w, y: 0 * h, w, h}
    ]
  },
  left: {
    idle: [
      { x: (1 + 0) * w, y: 0 * h, w, h}
    ],
    move: [
      { x: (1 + 0) * w, y: 0 * h, w, h}
    ],
    hurt: [
      { x: (1 + 0) * w, y: 0 * h, w, h}
    ],
    rekt: [
      { x: (1 + 0) * w, y: 0 * h, w, h}
    ],
    alarm: [
      { x: (1 + 0) * w, y: 0 * h, w, h}
    ]
  }
}