const w = 20
const h = 30

export default {
  right: {
    move: [
      { x: 0 * w, y: 0 * h, w, h },
      { x: 1 * w, y: 0 * h, w, h },
      { x: 2 * w, y: 0 * h, w, h },
      { x: 3 * w, y: 0 * h, w, h },
      { x: 4 * w, y: 0 * h, w, h },
      { x: 5 * w, y: 0 * h, w, h },
      { x: 6 * w, y: 0 * h, w, h },
      { x: 7 * w, y: 0 * h, w, h },
      { x: 8 * w, y: 0 * h, w, h },
      { x: 9 * w, y: 0 * h, w, h },
      { x: 10 * w, y: 0 * h, w, h },
      { x: 11 * w, y: 0 * h, w, h }
    ]
  }
}